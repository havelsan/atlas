

import {Injectable} from "@angular/core";


@Injectable()

export class CashOfficeFormsMethods{

    // public OpenForm(objectDefName: string,systemApiService: SystemApiService,selectedEpisodeID:Guid,activePatientService: IActivePatientService)
    // {
    //     if (selectedEpisodeID !== undefined && selectedEpisodeID !==null) {
    //         activePatientService.setActiveEpisode(selectedEpisodeID);
    //         //this.activePatientService.setActivePatient(this.selectedPatientID);
    //         systemApiService.new(objectDefName);
    //     }
    //      else
    //     {
    //         ServiceLocator.MessageService.showError("Lütfen protokol seçiniz!");
    //     }
    // }
}